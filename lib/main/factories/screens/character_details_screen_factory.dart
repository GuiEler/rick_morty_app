import 'package:flutter/widgets.dart';

import '../../../view/screens/screens.dart';

Widget makeCharacterDetailsScreen({required int characterId}) => CharacterDetailsScreen(characterId: characterId);
